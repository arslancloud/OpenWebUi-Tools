# OpenWebUi-Tools
A set of three OpenWebUI tools that connect your LLM to **Microsoft Outlook Mail**, **Outlook Calendar**          , and **SharePoint** using the Microsoft Graph API with delegated user permissions. All data access is s          coped to the signed-in user — the tools never access other users' accounts.
